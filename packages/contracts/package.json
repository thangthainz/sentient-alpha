{
  "name": "@sentient-alpha/contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "forge build",
    "test": "forge test -vvv",
    "deploy:testnet": "forge script script/Deploy.s.sol --rpc-url $MANTLE_TESTNET_RPC_URL --broadcast",
    "deploy:mainnet": "forge script script/Deploy.s.sol --rpc-url $MANTLE_RPC_URL --broadcast"
  }
}
